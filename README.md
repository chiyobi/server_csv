# server_csv

1. install node modules: `npm install`
2. update `.env` file with values for `SMTP_USER`, `SMTP_PASS`, `BASE_URL`
3. run locally with hot reloading: `npm run dev`
4. to create /dist: `npm run build`